// Ejercicio9.js 
const sentences = ['Hello world', 'JavaScript is fun', 'I love coding', 'Keep it simple'];
const filtrado = sentences.filter(el => el.length > 12);
console.log(filtrado.length, filtrado);